# secrets — never commit the Dhan API token
.dhan_token

# live-recorded chain snapshots (200MB+/day, over GitHub's file limit)
data/chain/

# caches / local tooling state
__pycache__/
*.pyc
.pytest_cache/
.claude-flow/
.claude/settings.local.json

# server logs
*.log
